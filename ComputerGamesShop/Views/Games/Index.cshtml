@model IEnumerable<ComputerGamesShop.Models.Game>

@{
    ViewData["Title"] = "Index";
}

<link rel="stylesheet" href="~/css/games.css" type="text/css" />
<p>
    <a asp-action="Create">Create New</a>
</p>
@foreach (var item in Model) {
    <div class="game">
        <div class="game-img">
            <img src="@Html.DisplayFor(modelItem => item.Image)">
        </div>
        <div class="content">
            <span class="header">@Html.DisplayFor(modelItem => item.Title)</span>
            <span class="publisher">@Html.DisplayFor(modelItem => item.Publisher.Name)
            </span>
            <span class="description">
                @Html.DisplayFor(modelItem => item.Description)
            </span>
        </div>
    </div>

            @*<td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsMultiplayer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Publisher.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>*@
}